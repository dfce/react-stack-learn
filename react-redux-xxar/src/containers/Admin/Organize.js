import {connect} from 'react-redux';
import Organize from '../../components/Admin/Organize';
// import * as 

const mapStateToProps = state => state;

export default connect(mapStateToProps)(Organize);